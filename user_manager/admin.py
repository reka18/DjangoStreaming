from django.contrib import admin

# from user_manager.models import StreamUser

# Register your models here.

# We do not register StreamUser since it's management
# handled by the existing django User interface.
# admin.site.register(StreamUser)
